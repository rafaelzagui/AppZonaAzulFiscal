<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="CoarseFineLocation">

    <uses-feature android:name="android.hardware.camera.any" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppFiscalZonaAzul">

        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCyNhMXePyDSwWJuXEcU8bAUYq8qzZFfKc" />

        <activity
            android:name=".TelaMapsActivity"
            android:exported="false"
            android:label="@string/title_activity_tela_maps"
            android:theme="@style/Theme.AppFiscalZonaAzul.NoActionBar"/>
        <activity
            android:name=".TelaItinerarioActivity"
            android:exported="false"
            android:theme="@style/Theme.AppFiscalZonaAzul.NoActionBar" />
        <activity
            android:name=".CameraPreviewActivity"
            android:exported="false"
            android:theme="@style/Theme.AppFiscalZonaAzul.NoActionBar" />
        <activity
            android:name=".TelaRegistrarIrregularidadeActivity"
            android:exported="false"
            android:theme="@style/Theme.AppFiscalZonaAzul.NoActionBar" />
        <activity
            android:name=".TelaConsultaActivity"
            android:exported="true"
            android:theme="@style/Theme.AppFiscalZonaAzul.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>